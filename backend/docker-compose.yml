services:
  postgres:
    image: postgres:16-alpine
    container_name: job-dashboard-db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=job_dashboard
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    env_file:
      - .env
    environment:
      # Changed '@db' to '@postgres' to match your service name above
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/job_dashboard?sslmode=disable
    deploy:
      replicas: 3
    depends_on:
      - postgres

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - app

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    depends_on:
      - app

  grafana:
    image: grafana/grafana:11.2.0
    container_name: grafana
    platform: linux/amd64
    ports:
      - "3000:3000"
    depends_on:
      - prometheus

volumes:
  postgres_data: